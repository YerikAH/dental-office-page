export enum InputTypes{
  DATE,
  HOUR,
  EMAIL,
  SELECT,
  TEXT,
  NUMBER,
  PASSWORD
}
export enum RoutesEnum{
  HOME = "/",
  OUR = "/our",
  CONTACT = "/contact",
  SERVICES = "/services",
  APPOINTMENT = "/appointment",
  PRODUCTS = "/products",
  PRODUCT = "/products/:id",
}