export interface NavigationItems {
  path: string;
  label: string;
  active: boolean;
}
export interface SocialMedia {
  media: string;
  url: string;
}
export interface Contacts{
  label: string
  icon: string
}
